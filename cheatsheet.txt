Gruppe1:
git status
git add <datei>

Gruppe2:
git commit -m "Deine Commit-Nachricht": Änderungen aus Stageing area auf lokales Repo übernehmen. Zusäzliche Nachricht mit Infos über die Änderungen.
git pull origin main: Den aktuellen Stand vom Repository ziehen und in lokale Umgebung übertragen.

Gruppe3:
git push origin main - upload local repository content to a remote repository
git branch - List, create or delete branches

Gruppe4:
git checkout <branchname>
git merge <branchname>

Gruppe5:
git log
git clone <repository-url>

Gruppe6:
git fetch origin
git diff

Gruppe7:
git remote -v
git rebase main

Gruppe8:
git reset <datei>
git stash

Gruppe9:
git init
git tag v1.0

Gruppe10:
git rm <datei>
git config --global user.name "Dein Name"